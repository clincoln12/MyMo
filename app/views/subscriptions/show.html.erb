<h1><%= @subscription.name %> - $<%= @subscription.fee %>/mo</h1>
<h2>Subscription started on <%= @subscription.billing.strftime('%m-%d-%Y') %></h2>

<%= link_to "Edit", edit_subscription_path %>
<%= link_to "Delete", subscription_path(@subscription), :confirm => "Are you sure?", :method => :delete %>

<%= link_to "Add Payment", new_subscription_payment_path(@subscription) %>
<%= link_to "View Payments", subscription_payments_path(@subscription) %>